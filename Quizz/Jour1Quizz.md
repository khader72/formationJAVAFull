
# Quiz 1 : Jour 1 - Fondamentaux de Java

1. Qu'est-ce que Java ?
   
   a) Un café
   
   b) Un langage de programmation
   
   c) Un système d'exploitation
   
   d) Un outil de design graphique
   
   e) Un animal
2. Quelle est la différence entre un JDK et un JRE ?
   
   a) JDK est une extension de fichier, JRE est une boisson
   
   b) JDK contient un compilateur, JRE contient un exécuteur
   
   c) JDK signifie "Java Developer Kit," JRE signifie "Java Runtime Environment"
   
   d) Il n'y a aucune différence
   
   e) JDK est utilisé pour la navigation web, JRE pour le développement Java
3. Comment déclarez-vous une variable en Java ?
   
   a) variable = 10;
   
   b) createVariable(10);
   
   c) int variable = 10;
   
   d) setVariable(10);
   
   e) new Variable(10);
4. Quelle est la signification de "public static void main(String[] args)" ?
   
   a) C'est une déclaration de variable
   
   b) C'est une méthode pour imprimer "Hello, World!"
   
   c) C'est une méthode principale (point d'entrée) du programme Java
   
   d) C'est un commentaire
   
   e) C'est une classe Java standard
5. Comment imprimer du texte dans la console en Java ?
   
   a) Utiliser un photocopieur
   
   b) System.print("Hello, World!");
   
   c) Console.log("Hello, World!");
   
   d) System.out.println("Hello, World!");
   
   e) Text.print("Hello, World!");
6. Quelle est la différence entre int et double en termes de types de données ?
   
   a) Il n'y a pas de différence
   
   b) int peut stocker des nombres décimaux, double ne le peut pas
   
   c) int est un type de données, double est une classe
   
   d) int stocke des nombres entiers, double stocke des nombres à virgule
   
   e) double est utilisé pour les chaînes de caractères
7. Qu'est-ce qu'une boucle "for" en Java ?
   
   a) Un type de condition if-else
   
   b) Une structure de données
   
   c) Une boucle qui s'exécute un nombre fixe de fois
   
   d) Une méthode pour trier un tableau
   
   e) Un fichier de configuration
8. Comment comparez-vous deux chaînes (String) en Java ?
   
   a) En utilisant la classe StringComparer
   
   b) Avec l'opérateur "=="
   
   c) En utilisant la méthode compare()
   
   d) En utilisant l'opérateur ".equals()"
   
   e) Vous ne pouvez pas comparer des chaînes en Java
9. Qu'est-ce qu'une classe en Java ?
   
   a) Un objet
   
   b) Une variable
   
   c) Une fonction
   
   d) Une instruction
   
   e) Un modèle pour créer des objets
10. Comment créer une instance d'une classe ?
    
    a) En utilisant le mot-clé "class"
    
    b) En appelant la méthode "new"
    
    c) En utilisant "createInstance()"
    
    d) En utilisant "getNewInstance()"
    
    e) Vous ne pouvez pas créer d'instances de classe en Java
11. Quelle est la différence entre l'héritage et l'interface en Java ?
    
    a) Aucune différence
    
    b) L'héritage permet à une classe d'hériter des membres d'une autre classe, tandis qu'une interface définit un contrat que les classes doivent implémenter
    
    c) L'interface permet d'hériter des membres d'une autre classe, tandis que l'héritage définit un contrat
    
    d) L'héritage est utilisé pour copier du code entre les classes, tandis que l'interface est utilisée pour l'encapsulation
    
    e) L'héritage est utilisé pour créer des instances de classe, tandis que l'interface est utilisée pour créer des tableaux
12. Quand devez-vous utiliser "try-catch" pour la gestion des exceptions ?
    
    a) Toujours
    
    b) Jamais
    
    c) Uniquement pour les erreurs de syntaxe
    
    d) Uniquement pour les erreurs de logique
    
    e) Lorsque vous prévoyez des erreurs potentielles dans votre code
13. Qu'est-ce que la surcharge de méthode (method overloading) ?
    
    a) Une erreur dans le code
    
    b) Une façon de définir des méthodes identiques dans une classe
    
    c) Une méthode qui a trop de paramètres
    
    d) Une méthode qui renvoie plusieurs valeurs
    
    e) Une méthode qui utilise une surcharge électrique
14. Comment définissez-vous une constante en Java ?
    
    a) En utilisant le mot-clé "constant"
    
    b) En mettant un "$" devant le nom de la variable
    
    c) En utilisant le mot-clé "final"
    
    d) En utilisant le mot-clé "immutable"
    
    e) Les constantes ne sont pas autorisées en Java
15. Quel est l'opérateur ternaire en Java ?
    
    a) Un opérateur pour les calculs trigonométriques
    
    b) Un opérateur qui compare trois valeurs
    
    c) Un opérateur pour les opérations sur les chaînes de caractères
    
    d) Un opérateur pour les expressions conditionnelles (condition ? valeurSiVraie : valeurSiFausse)
    
    e) Un opérateur qui transforme trois valeurs en une seule
16. Qu'est-ce qu'un tableau (array) en Java ?
    
    a) Un type de café
    
    b) Une collection de valeurs de même type
    
    c) Une classe standard de Java
    
    d) Une méthode pour trier des données
    
    e) Un nom de variable
17. Comment déclarez-vous un tableau en Java ?
    
    a) En utilisant le mot-clé "tableau"
    
    b) En mettant des crochets autour du type de données (par exemple, "int[]")
    
    c) En utilisant le mot-clé "collection"
    
    d) En utilisant le mot-clé "ensemble"
    
    e) En utilisant le mot-clé "liste"
18. Comment accédez-vous aux éléments d'un tableau en Java ?
    
    a) En utilisant un pointeur
    
    b) En utilisant l'opérateur "->"
    
    c) En utilisant des indices (par exemple, "tableau[0]")
    
    d) En utilisant la méthode "getElement()"
    
    e) En utilisant la méthode "get()"
19. Qu'est-ce que l'autoboxing en Java ?
    
    a) Une méthode pour emballer des cadeaux
    
    b) Une technique pour réduire la taille des boîtes
    
    c) Une méthode pour encapsuler des objets dans une boîte
    
    d) Une conversion automatique entre les types primitifs et les objets (par exemple, int à Integer)
    
    e) Une manière de réorganiser les éléments d'un tableau
20. Qu'est-ce qu'une interface en Java ?
    
    a) Un menu utilisateur
    
    b) Un écran d'accueil
    
    c) Un outil pour interagir avec l'utilisateur
    
    d) Un contrat qui définit un ensemble de méthodes que les classes doivent implémenter
    
    e) Une classe abstraite
